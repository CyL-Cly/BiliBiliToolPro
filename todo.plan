# 大会员大积分 / 观看剧集 — 待完善

日期：2026-08-25（2026-09-18 重写：e195803 + T4 闭环大部分条目）
范围：VipBigPoint（ogvwatchnew + combine 反序列化 + 大会员经验观看）
已排除：签到、会员购、追番/影视浏览、装扮商城（无 access_key 时均已成功）

---

## 已证实（不要再按旧假设改）

- 过期 `access_key` → `receive/v2` / `complete/v2` 返回 `-400`；**不配 access_key 反而能过**。
- `POST /pgc/activity/score/task/complete/v2`（`taskCode=ogvwatchnew`）会返回 `code:0 success`，但是**假成功**：积分不变，`combine` 里 `state` 仍为 1。
- 网页 `POST /x/click-interface/web/heartbeat` 只写观看记录，**不计入**大积分「观看剧集」。
- `deliver/task/complete` 不带 `taskId` → `-400 taskId不能为空`。
- `material/receive` **没有下线**：用有效 Cookie 可返回 `watch_count_down_cfg.task_id/token`（`task_id` 固定 `4320003`，`token` 每次变，`milliseconds=600000`）。
- 拿到 `task_id/token` 后**立刻** `deliver/task/complete` → `-400 请求错误`。说明 complete 会校验「已连续观看约 10 分钟」，不是领完就能报。

现行真实流程（已实现；complete 成功包尚未抓到）：

```
receive/v2(ogvwatchnew)
  → material/receive 拿 task_id + token
  → APP 心跳把连续观看做实（已接入 /x/report/heartbeat/mobile，60s 间隔真实推进）
  → deliver/task/complete(task_id, token, task_sign)
  → complete 后以 combine 复核为准：state==3 且积分 +40，不要信 complete/v2 的 success
```

---

## 已完成（2026-09-18，e195803 + T4）

- `Sing_task_item` required 炸 combine → 已删属性 + 孤儿类，combine/签到 required 全可空并加守卫（e195803）。
- 过期 `access_key` 参与签名 → 已随 04c2121 移除 access_key 依赖与告警。
- ogvwatchnew 跳过心跳 → 已实现真实推进：60s/步、target=Clamp(countdownMs/1000,15,1800)+30s 缓冲、ct 透传（e195803）。
- duration 单位 → 实测毫秒（`duration>=10000` 换算 /1000，兼容旧秒级）（e195803）。
- complete 后无复核 → 已加 combine 复核，`ogvwatchnew.state==3` 才算完成（e195803）。
- 生产「缺少 task_id/token」日志无原文 → 已带 ToJsonStr；Refit 绑定已验证无问题（属性名一致，Web 默认大小写不敏感）。
- 死代码 WatchBangumi/GetBangumi、配置 ViewBangumis 已清理；bilitool bat/ps1 中文注释已英文化（e195803）。

---

## 现有错误 / 不完整实现

### 1. 大会员经验观看（VipExpress）脆弱

- `VipExpressAsync`：`privilege/my` state=2 → `WatchAndShareVideo`（日常投稿观看，可能因今日已看而跳过）→ 立刻 `experience/add` → 再 `GetCombineAsync`。
- 失败日志只有 `deserializing the response`，未打 URL。可能是 combine 缺 `sing_task_item`，也可能是排行榜/视频详情 required 字段。
- 未模拟「观看满约 1 分钟」的大会员经验条件。

---

## 待办

1. 大会员经验观看：确认反序列化失败的具体接口；combine 容错已就位，再看是否还要单独修观看。

---

## 还缺的抓包（只抓这些）

必须用 **大陆版手机 APP**，从
`我的 → 会员中心 → 赚大积分 → 观看剧集内容` 入口点进去播（不要网页、不要自己搜剧）。

过滤：`heartbeat` / `deliver` / `complete` / `material/receive` / `ogvwatch`

每条都要：完整 URL、Method、**全部请求头**、Query/Body、完整响应 JSON。
Cookie / access_key **值打码，字段名保留**。`task_id` / `token` / `task_sign` / `sign` / 响应结构不要打码。

### 必抓（按时间顺序，一次完整成功流程）

| # | 时机 | 预期接口 | 要确认的事 |
|---|---|---|---|
| A | 点任务入口、选剧、点播放 | `POST /pgc/activity/deliver/material/receive` | 脚本已实现 receive 调用，重点是对照 headers/UA 差异：locale/channel/build 常量现与抓包样本不一致（脚本 `zh-Hans_HK`/`html5_app_bili`，样本 `zh_CN`/`bili`），评估是否要改；响应是否始终有 `watch_count_down_cfg` |
| B | 开始播放立刻 | `POST /x/report/heartbeat/mobile`（start） | 脚本已有心跳实现，对照字段差异（重点 `play_type` 是否携带）；`played_time`/`actual_played_time`/`session`/`epid`/`sid`/`start_ts`/`from_spmid` |
| C | 播放中（任意一条即可） | 同上 heartbeat | 脚本已有心跳实现，对照字段差异（重点 `play_type` 是否携带）；中间上报间隔、字段如何递增 |
| D | 倒计时结束 / 连看满约 10 分钟 | 实际触发的 complete | **最关键**。预期 `POST /pgc/activity/deliver/task/complete`。脚本现用 `task_id+token+task_sign`+毫秒 `timestamp`；对照 Body 字段有无出入、有无别的字段 |
| E | 完成后刷新任务页 | `GET /x/vip_point/task/combine` | 脚本已复核 state；确认 `ogvwatchnew.state` 成功时确为 3；`sing_task_item` 在不在 |

没有 **D 的成功响应**，complete 成功分支仍是两态推断（`-400`/成功）+ 复核兜底；B/C 不再阻塞（心跳已实现），但需对照字段（重点 `play_type`）。

### 可选

- 大会员经验：观看任意视频约 1 分钟再领经验，失败那条的 URL + 响应（对应 `privilege/my` / 心跳 / `experience/add` / `combine`）。
- APP 版本 `build`/`version`、host 是 `api.bilibili.com` 还是 `app.bilibili.com`。

### 不用抓

receive/v2、装扮商城 complete/v2、签到、会员购、追番、影视、网页 `click-interface/web/heartbeat`。

---

## 不要做的事

- 不要把网页 heartbeat + `complete/v2` 当成 ogv 完成路径。
- 不要在无 APP 心跳、无成功 complete 抓包的情况下改死脚本。
- 不要把过期 `access_key` 继续塞进签名。
- 不要用 complete 接口自己的 `code==0` 作为 ogv 完成依据。

---

## 已按 2026-09-18 修复后仍可能的残余风险

- complete 成功响应包仍未抓到：现按 `-400`/成功两态处理 + combine 复核兜底。
- 开场心跳未带 `play_type`。
- 心跳 `timestamp` 毫秒单位未验证。
- locale/channel 指纹未对齐（脚本 `zh-Hans_HK`/`html5_app_bili` vs 样本 `zh_CN`/`bili`）：有意不动，等抓包。
